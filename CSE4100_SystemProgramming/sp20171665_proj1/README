프로그램명: Project1
프로그램 관련 코드 소스: 20171665.c / 20171665.h
작성자: 20171665학번 이선호

Linux shell 환경에서 command line으로 make를 입력할 때, 이 프로그램은 다음과 같이 컴파일된다.
1) C언어 컴파일러인 gcc(GNU C Compiler)에 의해 preproccessor가 실행되어 20171665.c와 20171665.h에서 include로 선언한 헤더 또는 라이브러리 파일을 읽어서 포함시키고 define으로 지정한 예약어를 구문의 상수로 치환한다.
2) 컴파일러에 의해 20171665.c가 어셈블리어 코드로 변환되고, 내부적으로 Linux의 assembler인 as를 호출함으로써 오브젝트 파일인 20171665.o이 생선된다. 경고를 출력하고 실행파일 이름을 20171665.out으로 설정하기 위해 gcc의 옵션에서 각각 -W와 -o를 추가했다.
3) Linux의 linker인 ld를 호출함으로써 헤더파일과 20171665.c 파일을 linking 시켜서 실행파일 20171665.out을 만든다. 이후 rm 명령어를 통해 오브젝트 파일인 201711665.o를 삭제한다.

소스 코드를 컴파일한 후 생성된 20171665.out을 Linux 환경에서 다음과 같이 실행한다.
./20171665.out

프로그램은 처음 실행할 때 opcode txt 파일을 읽는다. 따라서 opcode.txt 파일이 실행 파일이 존재하는 디렉토리에 위치해 있지 않거나 읽는 과정에서 오류가 발생하면 Error를 출력하고 종료한다. 
정상적으로 파일의 데이터를 모두 읽으면 사용자로부터 command line을 입력받는다. 유효한 command 입력과 형식은 help  또는 h를 입력하면 확인할 수 있다. 
quit 또는 q를 입력하면 동적할당한 메모리가 모두 해제되고 프로그램이 종료된다.